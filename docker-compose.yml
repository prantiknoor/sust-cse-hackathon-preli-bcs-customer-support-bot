services:
  api:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - GEMINI_MODEL=${GEMINI_MODEL:-gemma-4-31b-it}
      - PORT=8000
    env_file:
      - .env
    restart: always
